# hello-first-pr

A tiny practice project for shipping a first PR.

`greet.sh` defines a `greet` function that prints a greeting for a given name,
defaulting to "World" if no name is given.

Run the tests with:

```sh
bash test.sh
```
